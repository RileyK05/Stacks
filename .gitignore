# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Local course data and generated artifacts (never committed)
data/raw/
# Local model runtime + model files (dev checkout)
data/runtime/
data/models/
data/processed/
# The local SQLite database (dev checkout; the desktop app uses the OS
# per-user data directory instead)
*.db
*.db-wal
*.db-shm
runs/

# OS
.DS_Store
Thumbs.db
.pytest_tmp/
