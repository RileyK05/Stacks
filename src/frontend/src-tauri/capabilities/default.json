{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the app's own window may ask of the shell.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:allow-open-url",
    "opener:allow-reveal-item-in-dir",
    "dialog:allow-open",
    "dialog:allow-save"
  ]
}
