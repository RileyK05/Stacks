# Empty = same origin: the backend serves this SPA and the API at /api
# (the Vite dev server proxies /api to it). Set an absolute URL only when
# the SPA is served from somewhere else.
PUBLIC_API_BASE=""
