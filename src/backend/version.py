"""The app's name and version. `scripts/set_version.py` keeps this file,
pyproject.toml, the frontend's package.json and the Tauri crate in step;
tests/test_version.py fails if they drift."""

APP_NAME = "Stacks"
__version__ = "0.2.0"
